## 1. Layout change

- [x] 1.1 In `src/app/products/[id]/page.tsx`, remove the `product.description` block from the right-hand text column.
- [x] 1.2 Render the `product.description` block directly under the product image in the left column, keeping the existing conditional (only render when a description is present).

## 2. Verification

- [x] 2.1 Visually spot-check a product with a description and a product without one on the product detail page.
